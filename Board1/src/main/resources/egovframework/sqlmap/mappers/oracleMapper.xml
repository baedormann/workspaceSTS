<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.example.sample.service.OracleMapper">
	
	<!-- 게시물의 댓글 목록 조회 -->
	<select id="selectReplyList" resultType="egovframework.example.sample.VO.ReplyVO">
		SELECT 
			ID
			, REFERENCE_BOARD 
			, REFERENCE_REPLY
			, WRITER
			, CONTENT
			, CREATE_DATE
			, IS_PRIVATE
		FROM REPLY
		WHERE USE_YN = 'Y'
		AND REFERENCE_BOARD = #{id}
		ORDER BY ID
	</select>
	
	<!-- 댓글 등록 -->
	<insert id="insertReply">
		INSERT INTO C##BOARD_ADMIN.REPLY (
			ID
			, REFERENCE_BOARD
			, REFERENCE_REPLY
			, WRITER
			, CONTENT
			, CREATE_DATE
			, IS_PRIVATE
			, PASSWORD
			, USE_YN
		) VALUES (
			id_seq.NEXTVAL
			, #{reference_board}
			, DEFAULT
			, #{writer}
			, #{content}
			, DEFAULT
			, DEFAULT
			, #{password}
			, DEFAULT
		)
	</insert>
	
</mapper>

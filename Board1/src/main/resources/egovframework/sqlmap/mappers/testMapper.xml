<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.example.sample.service.TestMapper">
	<!-- 게시판 목록 조회 -->
	<select id="selectBoardList" resultType="egovframework.example.sample.VO.TestVO">
		SELECT ID
			, WRITER
			, TITLE
			, CONTENT
			, DATE_FORMAT(CREATE_DATE, '%Y. %m. %d.') AS CREATE_DATE
			, IS_PRIVATE
		FROM BOARD1
		WHERE USE_YN = 'Y'
		ORDER BY ID DESC
	</select>
	
	<!-- 게시글 상세 조회 -->
	<select id="selectBoardDetail" resultType="egovframework.example.sample.VO.TestVO">
		SELECT ID
			, WRITER
			, TITLE
			, CONTENT
			, DATE_FORMAT(CREATE_DATE, '%Y. %m. %d.')
			, IS_PRIVATE
		FROM BOARD1
		WHERE ID = #{id}
	</select>
	
	<!-- 게시글 등록 -->
	<insert id="insertBoard">
		INSERT INTO BOARD1 
			(WRITER
			, TITLE
			, CONTENT
		)VALUES(
			#{writer}
			, #{title}
			, #{content}
		)
	</insert>
	
	<!-- 게시글 삭제 -->
	<update id="deleteBoard">
		UPDATE BOARD1
		SET USE_YN = 'N'
		WHERE ID = #{id};
	</update>

	<!-- 게시글 수정 -->
	<update id="updateBoard">
		UPDATE BOARD1
		SET TITLE = #{title}
			, CONTENT = #{content}
		WHERE ID = #{id};
	</update>
	
	<!-- 게시물의 댓글 목록 조회 -->
	<select id="selectReplyList" resultType="egovframework.example.sample.VO.ReplyVO">
		SELECT 
			ID
			, REFERENCE_BOARD 
			, REFERENCE_REPLY
			, WRITER
			, CONTENT
			, DATE_FORMAT(CREATE_DATE, '%Y. %m. %d.')
			, IS_PRIVATE
		FROM REPLY
		WHERE USE_YN = 'Y'
		AND REFERENCE_BOARD = #{id}
		ORDER BY ID
	</select>
	
	<!-- 댓글 등록 -->
	<insert id="insertReply">
		INSERT INTO REPLY (
			REFERENCE_BOARD
			, REFERENCE_REPLY
			, WRITER
			, CONTENT
			, CREATE_DATE
			, IS_PRIVATE
			, PASSWORD
			, USE_YN
		) VALUES (
			#{reference_board}
			, DEFAULT
			, #{writer}
			, #{content}
			, DEFAULT
			, DEFAULT
			, #{password}
			, DEFAULT
		)
	</insert>
</mapper>
